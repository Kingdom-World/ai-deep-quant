# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# 依赖与构建产物
node_modules
dist
dist-ssr
*.local

# 环境变量与本地配置（Vercel 部署无需 .env，前端 API 使用同源相对路径 /api）
.env
.env.*

# 自检报告
reports/

# SQLite 缓存数据库（本地 Baostock 缓存，不入库）
python_backend/data/

# Python 字节码构建产物（不入库；此前被追踪过，已用 git rm --cached 解除）
__pycache__/
*.pyc
*.pyo

# 根目录游离临时文件（AI 问答测试样本，非项目资产）
/t1.json
/t2.json

# Vercel
.vercel

# 编辑器文件
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# 凭据备忘（不入库）
访问账号密码.md

# 运行时数据（账本/会话密钥/用户表）不入库
data/
server.log

# 临时工作区（含登录 cookie、测试凭据、诊断脚本、协商中间稿）不入库
.tmpdir/

# 项目记忆与智能体配置不入库
.workbuddy/
.zcode/
AGENT-MEMORY.md

# 本地归档备份
backups/
