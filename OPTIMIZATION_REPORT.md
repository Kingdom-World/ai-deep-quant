# AI深度量化 v2.0 — 平台优化与独立性报告

> 项目: AI深度量化（原 my-react-app AI 量化平台）
> 本次版本：从「多进程依赖」全面升级为「单端口独立平台」

## 本次全局整改清单（v2.0）

### 1. 平台更名 AI深度量化
- 网页标题、导航栏、Hero 标语、页脚、后端服务名全部统一为 **AI深度量化**
- package.json 更名为 `ai-deep-quant`，版本 2.0.0

### 2. 主图/副图周期彻底分离
- **主图**：仅保留 日/3日/周/月/季/年（分钟周期全部移除，相关代码清理）
- **副图**：独占 1/5/15/30/60/120 分钟 K 线

### 3. 分钟线质量升级（真实多日数据）
- 新增后端 `/api/mkline`：A股=腾讯 mkline、美股=新浪 US_MinKService、港股=分时聚合
- 旧版用当日分时本地拼凑（60分仅约 4 根），新版为真实多日 OHLCV（320 根）
- 渲染增强：MA5/10/20、最新价虚线标线、成交量红绿柱、缩放滑块、MM-DD HH:mm 时间轴、富文本悬浮框

### 4. 市场概况修复
- 前端指数占位代码与后端 symbol 形态统一（sh000001/usINX 等）
- 匹配逻辑归一化（剥离 sh/sz/hk/us 前缀），美股指数此前匹配失败 → 现已正常显示
- 指数卡片点击直达对应指数看板

### 5. 独立单 URL 运行
- `server/index.cjs` 一体化：API + dist 静态托管（含 gzip 预压缩）+ SPA 路由回退
- **一个端口 3001 即整站**：`npm start` 或双击 `start.bat`（自动装依赖→构建→启动→开浏览器）
- 移除 MCP 桥接、stock-sdk、cheerio、@modelcontextprotocol/sdk 等全部遗留依赖

### 6. 阉割版维护模块（平台关闭也能自检）
- `server/index.cjs` 内置每日 02:00–03:00 自检调度
- `register-maintenance.bat` 注册 Windows 计划任务（每日 02:05，平台关闭也执行）
- 自检内容：代码语法扫描（node --check）+ 前端产物完整性 + 8 项接口冒烟测试
- 报告：`reports/maintenance-<日期>.json` + `maintenance.log`
- 手动触发：`npm run maintain`

### 7. 新增两大功能页
- **策略回测** `/backtest`：MA双均线/RSI/买入持有，收益曲线+年化+最大回撤+胜率+交易明细（0.1% 手续费）
- **AI 智能助手** `/assistant`：站内离线规则引擎——个股五因子解读、股票池推荐、使用指南

### 8. 后台任务清理
- 已杀除遗留的 MCP 桥接进程（3987 端口，文件早已删除但进程存活）
- 最终运行态：**仅 1 个服务进程**（node server/index.cjs @ 3001）

## 性能基线（v2.0 实测）
| 项 | 指标 |
| --- | --- |
| 前端构建 | ~1.6s（635 模块） |
| 后端冷请求（历史K线） | <0.1s |
| 缓存命中 | ~5ms（报价 5s TTL / 历史 5min / 分钟K线 1min） |
| gzip 传输 | echarts 1.09MB → 358KB（67% 压缩） |
| 自检全项 | 10/10 通过，<30s |

## 已知边界
- 港股分钟线为当日分时聚合（数据源限制），A股/美股为多日真实分钟数据
- AI 问答为离线规则引擎（无外部 AI API），解读基于真实行情量化指标
- 行情数据来自新浪/腾讯公开接口，仅供参考，不构成投资建议
