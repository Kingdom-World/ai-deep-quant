# 📋 AI深度量化平台 UI/UX 升级规划书

> 起草：2026-08-29（ZCode Agent）。**本文件为实施依据，经用户确认后分阶段执行，每阶段独立验收、独立 git 提交、可回滚。**

## 一、背景与目标

平台功能层（行情、回测、模拟交易、自检）已完备，本轮聚焦**体验层**升级，目标三个：

1. 模拟交易页实现"看行情 → 下单 → 看持仓 → 平仓"的页面内闭环；
2. 用现代登录页（参考 DeepSeek / GLM 官网风格）替换浏览器原生灰色弹窗；
3. 全站六页视觉与导航统一，向主流量化平台（TradingView / 同花顺 / 东方财富）的布局合理性看齐。

## 二、现状问题清单（实测确认）

| # | 问题 | 影响 |
| --- | --- | --- |
| 1 | 模拟交易页点击持仓代码会跳转到详情页，页面本身无行情上下文；下单是"盲下" | 用户无法边看行情边交易（用户反馈 #1） |
| 2 | 登录是浏览器原生 HTTP Basic 弹窗（灰白系统框，无法定制品牌与交互） | 观感差、无注册能力、无"退出登录"（用户反馈 #2） |
| 3 | 六个页面各自手写头部导航，返回入口样式不一，无全局搜索贯通 | 割裂感、维护重复代码 6 份（用户反馈 #3） |
| 4 | 色板/间距/字号散落在各页内联样式中，无统一设计规范 | 风格漂移，新页面难保持一致 |
| 5 | 下单表单无股票搜索联想，需要用户准确记忆代码 | 易输错 |

**已有资产（直接复用）**：`server/auth.cjs` —— 完整的注册/登录/Cookie 会话模块（scrypt 哈希、HttpOnly Cookie 7 天、.env 引导管理员、自带 Basic 兼容不影响自检脚本），只差接线；`dataService.ts` 的行情 API（报价/分时/日K/搜索）可直接喂给模拟交易页的行情面板。

## 三、方案设计（三阶段）

### 阶段一：模拟交易页 · 行情联动（先做，见效最快）

| 项 | 内容 |
| --- | --- |
| 行情面板 | 页面右侧新增固定行情卡片：点击持仓/委托/日志中的任意代码 → 展示该股实时报价（现价/涨跌幅/今开/最高/最低/成交量）+ 当日分时图 + 近 120 日 K 线缩略图 |
| 按现价下单 | 行情面板内一键「按现价买入 / 卖出」，自动填入左侧下单表单并提交 |
| 搜索联想 | 下单表单代码输入接入 `/api/search` 联想下拉，选中自动带出名称 |
| 交互保留 | 持仓代码点击改为"页内选中"（不再跳出页面）；详情页入口保留为名称旁的小图标 |

- **涉及文件**：`src/pages/PaperTradingPage.tsx`（主）、`src/api/dataService.ts`（仅类型，API 已有）
- **后端改动**：无
- **验收标准**：不离开 /paper 完成"看行情 → 下单 → 查持仓 → 平仓"全流程；点击任意代码 1 秒内出行情

### 阶段二：登录系统升级（替换浏览器弹窗）

| 项 | 内容 |
| --- | --- |
| 后端接线 | `server/index.cjs` 挂载 auth.cjs：`init()` + `ensureBootstrapAdmin(.env 账号)` + `/api/auth/*` 路由（register/login/logout/me）；用 auth 中间件替换现有 Basic 中间件；**401 不再返回 WWW-Authenticate 头**（根除浏览器弹窗），改为纯 JSON |
| 静态资源 | dist 页面资源不再强制鉴权（仅 `/api/*` 鉴权）——SPA 自身守卫路由，深链接不碎 |
| 登录页 | 新建 `src/pages/LoginPage.tsx`：现代深色玻璃拟态风——深蓝渐变背景 + 光晕粒子、居中毛玻璃卡片、品牌标识、登录/注册双 Tab、错误提示动画、回车提交 |
| 路由守卫 | App 加载时调 `/api/auth/me`：未登录渲染 LoginPage（无需跳转，无闪烁）；已登录进入正常页面 |
| 导航栏 | 右上角显示当前用户名 + 退出按钮 |
| 多用户 | 开放注册；模拟盘账户天然按 uid 隔离（broker 已按 uid 分账），每人独立的 100 万虚拟资金 |
| 兼容 | auth.cjs 自带 Basic 兼容：自检冒烟脚本与 vite 开发代理不受影响；.env 的 admin 账号首次启动自动引导创建 |

- **涉及文件**：`server/index.cjs`（小改）、`src/pages/LoginPage.tsx`（新）、`src/App.tsx`、TopNav、`访问账号密码.md`（更新说明）
- **验收标准**：全程无浏览器弹窗；未登录访问任意页 → 页内优雅呈现登录界面；登录后 7 天免登录；可注册第二个账号且模拟盘数据互相隔离

### 阶段三：全站 UI 统一升级（参照主流量化平台）

| 项 | 内容 |
| --- | --- |
| 统一导航 | 新建 `src/components/TopNav.tsx`：Logo + 六页导航（高亮当前页）+ 全局股票搜索框 + 用户菜单；替换六页手写头部（消除 6 份重复代码） |
| 设计规范 | 沉淀 `src/lib/theme.ts`：色板（背景 #0a0e17 / 卡片 #111827 / 主色 #60a5fa / 涨 #ef4444 跌 #22c55e —— A 股习惯红涨绿跌）、8px 间距网格、字号阶梯（12/13/14/18/22） |
| HomePage | 指数卡片可点击跳转对应指数详情；"今日观察"评分改为进度条可视化；功能中心入口对齐新样式 |
| StockDetailPage | 参照 TradingView 经典布局：顶部价格横幅（现价/涨跌/高低/量额常驻），左侧主图为绝对主体；**只做头部替换 + 价格横幅两个最小侵入点，不重构 1730 行内部逻辑**（控制风险） |
| BacktestPage | 结果指标卡片化，策略 vs 基准对比着色，交易明细表格化 |
| 其余页面 | AnalyzePage / AssistantPage / PaperTradingPage 对齐 TopNav 与卡片风格 |

- **涉及文件**：全部 pages + 新 `components/TopNav.tsx`、`lib/theme.ts`
- **验收标准**：六页导航一致、无视觉跳变；StockDetailPage 功能回归正常（K线/分钟副图/形态标注不破坏）

## 四、实施顺序与工作量预估

```
阶段一（模拟交易行情联动）→ 阶段二（登录系统）→ 阶段三（全站 UI，可按页拆分交付）
```

- 阶段一：改动集中在一个页面，约 1–2 小时代码量
- 阶段二：后端小改 + 一个新页面，约 2 小时
- 阶段三：面最广，约 3–4 小时，**按页拆分提交，每页完成后可单独验收**

每阶段收尾固定动作：`npm test`（18 项回归）→ `npm run build` → `npm run maintain`（自检 10 项）→ git commit。

## 五、风险与对策

| 风险 | 对策 |
| --- | --- |
| 登录方式切换导致用户"密码失忆" | .env 账号自动引导创建 + 《访问账号密码.md》同步更新；注册开放后可自助找回（重新注册新号） |
| StockDetailPage 体量大、重构易碎 | 阶段三对它只做两个最小侵入点，内部逻辑一律不动 |
| UI 改动无设计稿、容易改飞 | 以现有深色体系为基准做"统一与精修"，不做颠覆式改版；每页完成后截图对比验收 |
| 模拟盘多用户后的数据混淆 | broker 已按 uid 分账（现在就是单用户共用），切换后各账号独立；旧 admin 数据保留在 admin 名下 |

## 六、待用户确认事项

1. 是否按"阶段一 → 二 → 三"顺序全部实施？
2. 登录页风格：深色玻璃拟态（方案默认，类 DeepSeek/GLM 深色版）还是浅色简约？默认按深色执行（与平台整体一致）。
3. 阶段三 A 股配色按"红涨绿跌"执行（国内习惯），如需国际惯例"绿涨红跌"请指出。
